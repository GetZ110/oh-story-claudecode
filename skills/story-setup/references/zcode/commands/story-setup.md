---
description: Deploys the ZCode web-novel Skills, Commands, Hooks, and AGENTS.md for the current project.
skills: story-setup
---

Invoke `$story-setup` to deploy the current writing project with `target_cli=zcode`; preserve the user's existing configuration and merge safely.

User arguments: $ARGUMENTS
