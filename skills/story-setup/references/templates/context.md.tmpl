# Writing Progress — {BookTitle}

> Purpose: snapshot of the writing session's progress, for restoring context after compaction.
> This file stores progress metadata only; it does not duplicate the content of dedicated state files.
> For full context, read alongside: tracking/character-state.md, tracking/foreshadowing.md.

## Current Position
- Volume: _Volume·___
- Chapter: _Chapter
- Scene: ___
- Emotion target: ___→___ (_shape arc)
- Quality alert: ___

## Style Fingerprint
> Loaded before every chapter; set sentence-length rhythm from this before writing each chapter — do not copy a possibly drifted previous chapter. On first writing, fill from the "sentence-length distribution" snapshot.
> Source takes whichever has numbers: the custom setting/style.md if it has a sentence-length distribution, otherwise fall back to the benchmark style.md (even if downgraded to reference); with neither, leave blank and rely on the feel of the original anchor excerpts.
> Refresh this snapshot with the new source's sentence-length distribution only when the "source" changes (e.g. setting/style.md added or changed, authoritative style base switching from benchmark to custom); when the source is unchanged, keep it unchanged — do not get pulled by a drifted previous chapter.
- Target sentence-length band: Short <15 words _% / Medium 15-30 _% / Long >30 _% / Average _words / Mean segment _words
- Source: ___ (setting/style.md custom / benchmark style.md / original anchor excerpt)

## Recent Decisions
- [date] decision content → scope of impact

## Open Threads
- List only 2-3 threads relevant to the current scene; the full foreshadowing list lives in tracking/foreshadowing.md

## Changes This Session
- Record only the incremental changes of this writing session

## To-Do
- [ ] next step

## Layered Summary of Completed Content

> Enable the layered summary after 30 chapters. Leave this block empty before 30 chapters.
> The last 5 chapters need no dedicated block; write them directly into the "Recent decisions / Open threads / Changes this session" blocks above.

### Ten-Chapter Digest (one paragraph per 10 chapters)
<!-- Format: chapter range | core events written | character-state changes -->

### Volume-Level Overview (one sentence per volume)
<!-- Format: volume name | main-line progress written | key turning points -->
