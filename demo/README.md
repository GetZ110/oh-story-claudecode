# Demo Profiles

The English demos include book-level `AGENTS.md` files so they exercise the
same language, market, rating, and serialization contract as real projects.

The directory `demo/长篇/` is a legacy Chinese compatibility fixture used by
UTF-8 and migration tests. It is not part of the default English writing path.
